<Window x:Class="database.User"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        xmlns:vm="clr-namespace:database.ViewModel" 
        Title="User" Height="320" Width="400">
    <Window.DataContext>
        <vm:ViewUser/>
    </Window.DataContext>
    <Grid Margin="5,5,2,2">
        <TabControl HorizontalAlignment="Left" Height="272" VerticalAlignment="Top" Width="375">
            <TabItem Header="Find">
                <Grid>
                    <ComboBox Name="Pick_Category" Margin="196,51,21,165"/>
                    <Label Content="Table" HorizontalAlignment="Left" Margin="25,51,0,0" VerticalAlignment="Top" Width="137" Height="28"/>
                    <Button Content="Button" Command="{Binding ShowDataCommand}" Margin="131,180,131,35"/>
                    <TextBox HorizontalAlignment="Left" Height="23" Margin="131,135,0,0" TextWrapping="Wrap" Text="{Binding TextBox1, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Top" Width="107"/>
                </Grid>
            </TabItem>
            <TabItem Header="File New">
                <Grid>

                </Grid>
            </TabItem>

        </TabControl>
    </Grid>
</Window>
